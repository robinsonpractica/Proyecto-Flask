services:
  - type: web
    name: predictor-ingresos
    runtime: python
    buildCommand: pip install -r requirements.txt && python model.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
